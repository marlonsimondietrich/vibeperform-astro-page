---
import Button from "./Button.astro";
import NavBar from "./NavBar.astro";
import PrivacySecuritySection from "./PrivacySecuritySection.astro";
import RotatingHeadline from "./RotatingHeadline.astro";
import type { HomeContent } from "../data/homeContent";
import { SearchCheck, Workflow } from "lucide-static";

interface Props {
  content: HomeContent;
  currentLocale: string;
  alternateLocaleHref: string;
}

const { content, currentLocale, alternateLocaleHref } = Astro.props as Props;
const { nav, hero, features, audience } = content;
const basePath = "/vibeperform-astro-page";
const homeHref = currentLocale === "de" ? `${basePath}/de/` : `${basePath}/`;

const featureIcons = {
  "search-check": SearchCheck,
  workflow: Workflow,
} as const;

const renderFeatureIcon = (key?: keyof typeof featureIcons | string) => {
  const iconSvg =
    key && key in featureIcons
      ? featureIcons[key as keyof typeof featureIcons]
      : featureIcons["search-check"];

  return iconSvg
    .replace('class="lucide', 'class="lucide h-10 w-10')
    .replace('width="24"', 'width="40"')
    .replace('height="24"', 'height="40"');
};
---

<div class="flex min-h-screen flex-col bg-white">
  <NavBar
    nav={nav}
    homeHref={homeHref}
    alternateLocaleHref={alternateLocaleHref}
  />
  <main class="flex flex-1 flex-col gap-y-24 pb-20">
    <!-- Center RotatingHeadline inside hero title section -->
    <section id="about-us" class="section-spacing">
      <div
        class="container-responsive mx-auto max-w-4xl space-y-4 text-center"
      >
        
	<p class="text-xs font-semibold uppercase tracking-[0.35em] text-slate-500 sm:text-sm">
		{hero.kicker}
	</p>

	<h1 class="text-4xl font-semibold tracking-tight text-slate-900 sm:text-5xl lg:text-6xl">
		<RotatingHeadline words={hero.rotatingWords} />
	</h1>

	<p class="text-base text-slate-600 sm:text-lg md:text-xl">{hero.subtitle}</p>

	<div class="flex w-full flex-col items-center gap-3 pt-4 sm:flex-row sm:justify-center">
		<Button>{hero.primaryCta}</Button>
		{hero.secondaryHref && hero.secondaryCta ? (
			<a
				class="inline-flex min-h-[2.75rem] w-full items-center justify-center rounded-full border border-transparent px-4 text-sm font-semibold text-slate-700 hover:text-slate-900 sm:w-auto"
				href={hero.secondaryHref}
			>
				{hero.secondaryCta}
			</a>
		) : null}
	</div>
      </div>
    </section>

    <section id="activities" class="section-spacing bg-slate-50">
      <div class="container-responsive space-y-10">
        <div class="space-y-4 text-center md:text-left">
          <p
            class="text-xs font-semibold uppercase tracking-[0.35em] text-slate-500"
          >
            {features.kicker ?? "Our Approach"}
          </p>
          <h2 class="text-3xl font-semibold text-slate-900 sm:text-4xl">
            {features.heading}
          </h2>
          {
            features.subheading ? (
              <p class="max-w-2xl text-sm text-slate-600 sm:text-base">
                {features.subheading}
              </p>
            ) : null
          }
        </div>
        <div class="grid-responsive md:grid-cols-2">
          {
            features.items.map((item) => (
              <article
                class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm transition hover:-translate-y-1 hover:shadow-lg sm:p-8"
                key={item.title}
              >
                <div
                  class="mb-4 flex justify-center text-slate-500"
                  aria-hidden="true"
                  set:html={renderFeatureIcon(item.icon)}
                />
                <h3 class="text-lg font-semibold text-slate-900 sm:text-xl">
                  {item.title}
                </h3>
                {
                  item.blocks?.map((block, blockIndex) => {
                    if (block.type === "lead") {
                      return (
                        <p
                          class="mt-4 text-sm text-slate-600 sm:text-base"
                          key={`${item.title}-block-${blockIndex}`}
                          set:html={block.html}
                        />
                      );
                    }

                    if (block.type === "paragraph") {
                      return (
                        <p
                          class="mt-3 text-sm text-slate-600 sm:text-base"
                          key={`${item.title}-block-${blockIndex}`}
                          set:html={block.html}
                        />
                      );
                    }

                    if (block.type === "heading") {
                      return (
                        <p
                          class="mt-6 text-sm font-semibold text-slate-900 sm:text-base"
                          key={`${item.title}-block-${blockIndex}`}
                          set:html={block.html}
                        />
                      );
                    }

                    if (block.type === "list") {
                      return (
                        <ul
                          class="mt-3 space-y-2 text-sm text-slate-600 sm:text-base"
                          key={`${item.title}-block-${blockIndex}`}
                        >
                          {block.items.map((entry, entryIndex) => (
                            <li class="flex gap-2" key={`${item.title}-block-${blockIndex}-entry-${entryIndex}`}>
                              <span class="text-slate-400">•</span>
                              <span class="flex-1" set:html={entry} />
                            </li>
                          ))}
                        </ul>
                      );
                    }

                    return null;
                  })
                }
              </article>
            ))
          }
        </div>
        {
          features.cta ? (
            <div class="mx-auto max-w-3xl space-y-6 rounded-3xl bg-white p-8 text-center shadow-sm sm:p-10">
              <h3 class="text-2xl font-semibold text-slate-900 sm:text-3xl">
                {features.cta.heading}
              </h3>
              <div class="flex flex-col gap-3 sm:flex-row sm:justify-center">
                <a
                  class="inline-flex items-center justify-center rounded-full bg-slate-900 px-6 py-3 text-sm font-semibold text-white transition hover:bg-slate-700 sm:text-base"
                  href={features.cta.primary.href}
                >
                  {features.cta.primary.label}
                </a>
                <a
                  class="inline-flex items-center justify-center rounded-full border border-slate-200 px-6 py-3 text-sm font-semibold text-slate-700 transition hover:border-slate-300 hover:text-slate-900 sm:text-base"
                  href={features.cta.secondary.href}
                >
                  {features.cta.secondary.label}
                </a>
              </div>
            </div>
          ) : null
        }
      </div>
    </section>
    {
      audience ? (
        <section class="section-spacing">
          <div class="container-responsive space-y-8">
            <div class="space-y-4 text-center">
              <h2 class="text-3xl font-semibold text-slate-900 sm:text-4xl">
                {audience.heading}
              </h2>
              {
                audience.subheading ? (
                  <p class="mx-auto max-w-3xl text-sm text-slate-600 sm:text-base">
                    {audience.subheading}
                  </p>
                ) : null
              }
            </div>
            <div class="grid-responsive md:grid-cols-3">
              {
                audience.cards.map((card, cardIndex) => (
                  <article
                    class="h-full rounded-3xl border border-slate-200 bg-white p-6 text-left shadow-sm transition hover:-translate-y-1 hover:shadow-lg sm:p-8"
                    key={`audience-card-${cardIndex}`}
                  >
                    <p
                      class="text-base text-slate-700 sm:text-lg"
                      set:html={card.copy}
                    />
                  </article>
                ))
              }
            </div>
          </div>
        </section>
      ) : null
    }
    <PrivacySecuritySection
      badges={["DSGVO-konform", "Privacy by Design", "AV-Vertrag auf Anfrage"]}
    />
  </main>
</div>
